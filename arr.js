//using constructors
let array = new Array(5); // Creates an array with 5 empty slots
let numbers = new Array(1, 2, 3, 4, 5); // Creates an array with initial values
//using arr literals
let arr = [1, 2, 3, 4, 5]; // Array of numbers
let fruits = ["apple", "banana", "cherry"]; // Array of strings
//console.log shows result of value 0 ,1 and 2 in the given array 
console.log(fruits[0]); // "apple"
console.log(fruits[1]); // "banana"

let mixed = [1, "hello", true, null]; // Array of mixed data types
//initializing empty array
let emptyArray = [];//empty Array
